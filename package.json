{
  "name": "pern-todo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install)  ||  (cd client && npm run build)",
    "server": "node ./server/server.js",
    "client": "cd client && npm run dev"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
